//import will not work yet in JS, need to find another 
//solution, will require babel to run it, if not in react
//not using this as an example to run, just to demonstrate modules
//and classes

import { Teacher } from './teacher'

const teacher = new Teacher("Will", "Computer Science");

teacher.teach();